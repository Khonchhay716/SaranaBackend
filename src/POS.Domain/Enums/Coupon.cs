namespace POS.Domain.Enums.Coupon
{
    public enum DiscountType
    {
        Fixed = 1,
        Percentage = 2,
    }
    public enum ConditionType
    {
        MIN_QUANTITY = 1,
        MIN_AMOUNT = 2,
    }

    public enum Status
    {
        Active = 1,
        InActive = 2,
    }

}
